version: 2.1

workflows:
  version: 2
  build-and-run:
    jobs:
      - build-and-run-demo

jobs:
  build-and-run-demo:
    docker:
      - image: cimg/python:3.10

    steps:
      - checkout

      - run:
          name: Run Demo Script
          command: python demo.py

